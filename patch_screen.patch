--- feature/quiz-config/src/main/java/com/cglhustle/feature/quizconfig/presentation/QuizConfigScreen.kt
+++ feature/quiz-config/src/main/java/com/cglhustle/feature/quizconfig/presentation/QuizConfigScreen.kt
@@ -9,6 +9,7 @@
 import androidx.compose.ui.Modifier
 import androidx.compose.ui.unit.dp
 import androidx.hilt.navigation.compose.hiltViewModel
+import com.cglhustle.core.ui.state.UiState
 import com.cglhustle.feature.quizconfig.domain.model.QuizConfigPayload

 @Composable
@@ -39,7 +40,7 @@
             contentAlignment = Alignment.Center
         ) {
             when (val state = uiState) {
-                is QuizConfigUiState.Loading -> {
+                is UiState.Loading -> {
                     // Loading Skeleton (using a simple placeholder for now, avoid infinite spinner per guidelines)
                     Column(
                         horizontalAlignment = Alignment.CenterHorizontally,
@@ -49,16 +50,16 @@
                         LinearProgressIndicator()
                     }
                 }
-                is QuizConfigUiState.Error -> {
+                is UiState.Error -> {
                     Column(
                         horizontalAlignment = Alignment.CenterHorizontally,
                         verticalArrangement = Arrangement.spacedBy(8.dp)
                     ) {
-                        Text(text = state.message, color = MaterialTheme.colorScheme.error)
+                        Text(text = "An unexpected error occurred", color = MaterialTheme.colorScheme.error)
                         Button(onClick = { viewModel.loadFilters() }) {
                             Text("Retry")
                         }
                     }
                 }
-                is QuizConfigUiState.Success -> {
+                is UiState.Success -> {
                     if (isCreatingSession) {
                         Column(
                             horizontalAlignment = Alignment.CenterHorizontally,
@@ -69,7 +70,7 @@
                         }
                     } else {
                         QuizConfigContent(
-                            options = state.filters,
+                            options = state.data,
                             onStartQuiz = { payload ->
                                 viewModel.startSession(payload)
                             }
