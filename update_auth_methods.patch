--- feature/auth/src/main/java/com/cglhustle/feature/auth/AuthRoute.kt
+++ feature/auth/src/main/java/com/cglhustle/feature/auth/AuthRoute.kt
@@ -148,20 +148,20 @@
                     // Tabs
                     AuthTabs(
                         isSignUp = uiState.isSignUpMode,
-                        onTabSelected = { viewModel.setSignUpMode(it) }
+                        onTabSelected = { viewModel.onTabSwitched(it) }
                     )

                     Spacer(modifier = Modifier.height(24.dp))

                     EmailAuthForm(
                         uiState = uiState,
-                        onEmailChanged = { viewModel.updateEmail(it) },
-                        onPasswordChanged = { viewModel.updatePassword(it) },
-                        onConfirmPasswordChanged = { viewModel.updateConfirmPassword(it) },
-                        onNameChanged = { viewModel.updateName(it) },
-                        onAgeCheckedChanged = { viewModel.updateAgeChecked(it) },
-                        onPrivacyCheckedChanged = { viewModel.updatePrivacyChecked(it) },
-                        onSubmit = { if (uiState.isSignUpMode) viewModel.signUpWithEmail() else viewModel.signInWithEmail() }
+                        onEmailChanged = { viewModel.onEmailChanged(it) },
+                        onPasswordChanged = { viewModel.onPasswordChanged(it) },
+                        onConfirmPasswordChanged = { viewModel.onConfirmPasswordChanged(it) },
+                        onNameChanged = { viewModel.onNameChanged(it) },
+                        onAgeCheckedChanged = { viewModel.onAgeCheckedChanged(it) },
+                        onPrivacyCheckedChanged = { viewModel.onPrivacyCheckedChanged(it) },
+                        onSubmit = { viewModel.submitEmailForm() }
                     )

                     Spacer(modifier = Modifier.height(24.dp))
@@ -186,7 +186,7 @@
                             text = "Continue as Guest (Click Here)",
                             style = MaterialTheme.typography.bodySmall.copy(fontWeight = FontWeight.Medium),
                             color = MaterialTheme.colorScheme.onBackground.copy(alpha = 0.6f),
-                            modifier = Modifier.clickable { viewModel.signInAsGuest() }
+                            modifier = Modifier.clickable { viewModel.signInWithGuest() }
                         )

                         if (uiState.isGuestLoading) {
